local options = {
  ensure_installed = { "lua", "python", "c", "http", "javascript", "typescript" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
